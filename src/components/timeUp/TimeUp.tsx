import React from "react";
import Signal from "../signal/Signal";

const TimeUp = () => {
    const timeUpMessage = "Time's Up";

    return <Signal message={timeUpMessage} />;
};

export default TimeUp;
